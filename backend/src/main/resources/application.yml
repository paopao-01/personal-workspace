server:
  address: 127.0.0.1
  port: 8080
  servlet:
    context-path: /

spring:
  application:
    name: jobhub-backend
  datasource:
    url: jdbc:sqlite:${JOBHUB_DB_PATH:./data/jobhub.db}
    driver-class-name: org.sqlite.JDBC
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    baseline-version: 0
    execute-in-transaction: false
  mvc:
    throw-exception-if-no-handler-found: true

mybatis:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true
    default-fetch-size: 100
    default-statement-timeout: 30

jobhub:
  export-dir: ${JOBHUB_EXPORT_DIR:./data/exports}
  reminder-scan-delay-ms: ${JOBHUB_REMINDER_SCAN_DELAY_MS:60000}
  reminder-scan-initial-delay-ms: ${JOBHUB_REMINDER_SCAN_INITIAL_DELAY_MS:1000}

logging:
  level:
    com.jobhub: DEBUG
    org.flywaydb: INFO
    org.mybatis.spring: WARN
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
