# JobHub：Java 后端求职个人工作台 PRD

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 产品名称 | JobHub：Java 后端求职个人工作台 |
| 文档版本 | v1.0 |
| 产品类型 | 个人求职管理与能力提升工具 |
| 目标用户 | 有项目经历、正在持续投递的 0–2 年 Java 后端开发工程师 |
| 产品目标 | 帮助用户把每次投递和面试转化为下一次更有准备的行动 |
| 首期形态 | Web 应用，本地单用户模式优先 |
| 核心原则 | 基础人工流程可独立运行，AI 只提供可解释、可确认的增强建议 |

## 2. 产品概述

JobHub 是面向 Java 后端求职者的个人工作台，通过关联 JD、投递、面试、项目案例、问题和学习任务，建立以下闭环：

> JD 要求 → 确认能力差距 → 决定是否投递 → 面试准备 → 面试复盘 → 创建学习任务 → 下次面试前复习

产品首先帮助用户回答：

1. 这个岗位明确要求什么；我现在应投递、暂缓，还是忽略？
2. 面试前应该优先复习什么，并用哪些真实经历证明能力？
3. 多次面试中反复失分的知识点是什么；下一步要完成并验证什么任务？

产品不以生成看似精确的匹配分数为核心价值，而以可追溯的事实、低成本记录和持续复盘为核心价值。

## 3. 背景与用户问题

- 不同 JD 表述不一致，用户难以快速识别必须要求、加分项和隐含要求。
- 简历中的技能缺少项目证据，容易形成关键词堆砌或夸大表达。
- 投递、面试轮次、提醒和后续行动分散在多个工具中。
- 面试结束后容易忘记问题、回答状态和错误原因。
- 学习内容与目标岗位、面试问题之间缺少关联。
- 用户不清楚自己反复失分的领域，也无法验证是否真正改善。
- 传统求职管理工具首次录入成本较高，用户往往在获得价值前放弃。
- 用户虽记录了岗位和面试，但常常不知道“此刻最该做的下一步”。

## 4. 产品目标、原则与非目标

### 4.1 产品目标

1. 用户仅粘贴 JD 即可开始使用，不强制先完成完整个人画像。
2. 支持用户确认岗位要求，并区分“已满足、缺少证据、不满足、信息不足、待确认”。
3. 管理岗位、投递、面试日程和站内提醒。
4. 支持快速记录面试问题和回答状态，并形成可追溯的薄弱点。
5. 支持将薄弱点一键转为学习任务，形成最小改进闭环。
6. 将技能与真实项目、代码、文章、压测报告和面试表现关联。
7. 所有自动分析结果均可解释、可编辑、可拒绝，不静默修改用户事实。
8. 用户在首次使用的几分钟内，应能从一份 JD 得到明确的投递判断和准备动作。

### 4.2 产品原则

- **渐进式建档**：允许跳过非必要字段，资料不完整时仍可完成核心流程。
- **事实与建议分离**：用户输入和已确认事实不得被 AI 结果覆盖。
- **未知不等于不会**：缺少资料时展示“信息不足”，不自动判定为能力不足。
- **先闭环后自动化**：P0 不依赖 AI、邮件、浏览器通知或第三方日历。
- **行动优先**：进行中的投递与面试应始终有清晰的下一步行动；统计只服务于行动决策。
- **最小证据优先**：首次只要求可复用的项目案例摘要，代码、文章和附件等完整证据允许后补。
- **可追溯**：匹配结论、能力变化和任务来源均能回到原始记录。
- **数据归用户所有**：支持导出、归档和恢复，避免不可逆的数据丢失。

### 4.3 首期非目标

- 不做招聘平台，不抓取或批量投递岗位。
- 不做多人协作和企业招聘管理。
- 不做综合匹配百分比、自动能力评分和自动淘汰判断。
- 不做第三方日历双向同步。
- 不做浏览器通知、邮件和即时通信工具通知。
- 不做 AI 自动写入简历、能力等级或学习任务。
- 不做复杂权限系统；本地单用户模式不要求 JWT。
- P0 不做多实例提醒协调、复杂重试编排和跨端推送保证。

## 5. 用户与核心场景

### 5.1 核心用户

唯一核心角色为求职者。首期聚焦有至少一个可讲项目、正在持续投递且需要集中准备面试的 Java 后端求职者；用户在个人设备上使用本地单用户模式。

若后续支持公网部署或多设备同步，必须增加身份认证、会话管理和数据隔离，不能直接沿用本地单用户安全假设。

### 5.2 核心场景

#### 场景一：快速判断岗位

用户粘贴 JD，系统通过基础规则提取候选技能和要求。用户确认、修改或删除结果后，系统结合已有资料生成差距清单，并记录“投递、暂缓或忽略”的决定及理由，不输出综合百分比。

#### 场景二：管理投递和面试

用户创建投递记录和面试日程，系统展示未来 7 天面试、每条进行中投递的下一步行动，并按配置生成站内提醒。面试改期或取消时，提醒同步更新。

#### 场景三：快速复盘

用户在面试后记录结果、问题、回答状态和知识点。只填写问题及回答状态也可以保存，后续可继续补充完整复盘。

#### 场景四：形成学习闭环

用户把“部分答出”或“未答出”的问题一键转为带验证方式的学习任务。下次面试前，系统集中展示目标岗位要求、可用项目案例、历史问题和未完成任务。

## 6. 产品信息架构

```text
JobHub
├── 首页工作台
├── 岗位与投递
│   ├── 岗位列表
│   ├── JD 要求确认
│   ├── 差距清单
│   └── 投递记录
├── 面试中心
│   ├── 面试日程
│   ├── 站内提醒
│   ├── 面试准备包
│   ├── 快速/完整复盘
│   └── 问题与知识点
├── 学习任务
│   ├── 待办任务
│   ├── 薄弱知识点
│   └── 学习产出
├── 能力与证据
│   ├── 技能画像
│   ├── 项目库
│   └── 证据库
└── 设置
    ├── 时区与提醒
    ├── 数据导出
    └── 归档与最近删除
```

## 7. MVP 核心流程

```text
粘贴 JD
  → 确认少量关键岗位要求
  → 查看可解释的差距清单与投递建议依据
  → 记录投递/暂缓/忽略决定及下一步行动
  → 创建投递记录、面试和站内提醒
  → 在面试准备包中集中准备
  → 快速记录面试问题
  → 标记回答状态与知识点
  → 一键创建带验证方式的学习任务
  → 下次面试前复习并验证改进
```

P0 全部流程必须在 AI 服务不可用时正常运行。

### 7.1 首次使用最小闭环

用户不建立完整能力画像，也可在首次会话完成以下动作：粘贴一份 JD、确认至少 3 项关键要求（候选项不足时确认全部）、查看差距清单、为岗位选择“投递/暂缓/忽略”之一，并保存至少一项下一步行动。首次使用不要求上传附件或建立完整证据库。

## 8. P0 功能需求

### 8.1 首页工作台

首页聚合当前最需要行动的信息：

- 未来 7 天的面试日程。
- 已完成但尚未复盘的面试。
- 进行中的投递及其下一步行动。
- 即将到期或已逾期的学习任务。
- 最近 30 天高频薄弱知识点。
- 最近查看的岗位。

首页优先按时间敏感性展示“今天应做什么”：临近面试、待复盘、逾期任务、超过预期未推进的投递。核心操作包括新增岗位、新增面试、开始复盘、创建任务和查看面试准备包。空数据时展示下一步引导，不展示无意义的零分图表。

### 8.2 岗位与 JD 要求确认

#### 岗位字段

| 字段 | 必填 | 说明 |
|---|---:|---|
| 公司名称 | 是 | 招聘公司名称 |
| 岗位名称 | 是 | 如 Java 后端开发工程师 |
| JD 原文 | 是 | 用户粘贴的岗位描述 |
| 岗位来源 | 否 | 招聘网站、内推、官网等 |
| 岗位链接 | 否 | 原始 JD 地址 |
| 工作地点 | 否 | 城市、混合办公或远程 |
| 薪资范围 | 否 | 用户自行记录 |
| 决策状态 | 否 | TO_APPLY、APPLY、DEFER、IGNORE；用于记录投递判断，不替代投递状态 |
| 决策理由 | 否 | 如技术匹配、准备成本、地点或成长性 |
| 岗位状态 | 是 | ACTIVE、ARCHIVED |
| 备注 | 否 | 用户补充信息 |

岗位本身不保存投递状态，投递进度统一由投递记录维护。

#### 要求提取与确认

P0 可使用关键词、词典和规则提取候选要求。每项要求包含：

- 原始文本片段。
- 标准技能或能力项。
- 类型：必须要求、加分要求、工作职责、经验要求、业务领域、待确认。
- 用户确认状态：待确认、已确认、已忽略。
- 熟练程度描述（如 JD 中明确出现）。

用户可以确认、编辑、合并、删除候选要求。未经确认的候选结果不能作为确定性结论。

#### 差距清单

P0 不输出综合匹配百分比。每项已确认要求展示以下状态之一：

| 状态 | 定义 |
|---|---|
| 已满足且有证据 | 用户能力满足要求，并关联至少一项有效证据 |
| 自评满足但缺少证据 | 用户自评满足要求，但尚未关联证据 |
| 不满足 | 用户明确标记尚未掌握或等级不足 |
| 信息不足 | 用户尚未提供足够资料，不能判断 |
| 待确认 | 岗位要求或映射结果尚未由用户确认 |

缺少资料不得直接归为“不满足”。用户可以手动修正匹配状态，系统记录修正原因。

首次分析默认突出 3–5 项最影响投递或准备的要求。要求的重要性只能由 JD 明示规则或用户确认决定；“待确认”的候选项不得进入差距结论或准备优先级。

### 8.3 投递管理

投递字段包括岗位 ID、投递日期、渠道、当前状态、简历版本名称或附件引用、期望薪资、联系人、拒绝原因、下一步行动、下一步截止时间和备注。

每条进行中的投递应有一项可执行的下一步行动，例如补充材料、准备一面、等待结果或在指定日期跟进；状态变更、创建面试或完成复盘时可建议更新该行动，但不得静默覆盖用户填写的内容。

#### 投递状态

```text
DRAFT → APPLIED → RESUME_PASSED → INTERVIEWING → OFFER
                   └──────────────→ REJECTED
DRAFT/APPLIED/RESUME_PASSED/INTERVIEWING → WITHDRAWN
任一进行中状态 ↔ ON_HOLD
```

状态转换必须记录原状态、新状态、发生时间和备注。默认同一岗位只允许一条有效投递；用户确认后可以创建二次投递。

### 8.4 面试日程与站内提醒

#### 面试字段

| 字段 | 说明 |
|---|---|
| 投递记录 ID | 关联目标投递 |
| 面试轮次 | HR 面、一面、二面、终面或自定义 |
| 开始时间 | 以 UTC 保存 |
| 事件时区 | 创建时使用的 IANA 时区，如 Asia/Shanghai |
| 面试方式 | 线上、线下或电话 |
| 会议链接/地址 | URL 或地点 |
| 联系人 | HR 或面试官 |
| 日程状态 | SCHEDULED、COMPLETED、CANCELED、NO_SHOW |
| 面试结果 | PENDING、PASSED、FAILED |
| 准备事项 | 面试前检查清单 |
| 备注 | 补充信息 |

日程状态与面试结果分开保存。取消或缺席的面试不能设置通过或未通过结果。

#### P0 提醒规则

- 默认在面试前 1 天、2 小时、30 分钟生成站内提醒。
- 用户可以关闭、增加或修改提醒节点。
- 提醒唯一键为“面试 ID + 提醒类型 + 计划触发时间”。
- 修改面试时间后，取消未发送的旧提醒，并按新时间重新生成。
- 取消面试后，取消所有未发送提醒。
- 本地单用户模式下，定时任务定期生成与标记到期提醒；提醒记录须避免重复展示。
- P0 站内提醒以进入应用后可见为目标，失败原因可查看；P1 增加多实例租约抢占，避免同一提醒重复展示，并提供用户主动触发的失败重试；不承诺跨端送达保证。
- P0 站内提醒只保证用户进入应用后可见，不承诺应用关闭时的系统级推送。

提醒状态为 `PENDING / PROCESSING / SENT / FAILED / CANCELED`。

### 8.5 面试准备包

面试准备包是围绕单场面试生成的聚合视图，不单独复制业务数据。它至少展示：

- 目标岗位中已确认的必须要求、加分要求和仍待确认项。
- 与要求或知识点关联的项目案例和证据摘要；没有资料时明确显示“待补充”，不伪造案例。
- 同一知识点的历史面试问题、最近回答状态和高频失分记录。
- 关联岗位、该场面试和知识点的未完成学习任务，以及准备事项清单。
- 当前投递的下一步行动与截止时间。

准备包按面试时间展示“本场优先准备项”。优先级基于用户确认的岗位要求、临近面试时间、近期薄弱表现和任务截止时间，系统必须展示每条排序依据，不输出综合能力分数。

### 8.6 面试复盘

#### 快速复盘

必填项：面试结果，以及至少一道问题或明确选择“未记录到问题”。每道问题必须记录回答状态：完全答出、部分答出或未答出。

问题类型、知识点、我的回答、整体感受和下一步行动均可稍后补充。

#### 完整复盘

在快速复盘基础上增加面试难度、自我评分、面试官关注点、参考答案、错误原因、改进方案、岗位意愿，以及项目表达和真实性风险备注。

#### 复盘规则

- 每个面试日程只有一份当前复盘。
- 状态为 `NOT_STARTED / DRAFT / COMPLETED`。
- 草稿自动保存，完成后仍可修改，并记录最后修改时间。
- “已生成任务”不是复盘状态，通过关联任务数量和生成时间表示。
- 面试到达结束时间后只提示复盘，不自动将日程标记为已完成。

### 8.7 问题、知识点与薄弱点

每道问题包含问题内容、类型、我的回答、参考答案、回答状态、难度、错误原因、改进方案及多个知识点。

默认分类包括 Java 基础、集合、并发、JVM、Spring、MySQL、Redis、消息队列、分布式、系统设计、项目深挖、业务场景和 HR 问题，并允许自定义。

薄弱知识点统计口径：

- 在用户选择的时间范围内统计。
- “未答出”计 1 次，“部分答出”计 0.5 次，“完全答出”不计入薄弱次数。
- 统计结果必须能下钻到原始问题。
- 用户修改回答状态后，统计结果同步变化。
- 合并知识点后不得重复计数。

薄弱优先级不等同于累计次数。准备包中的排序应优先考虑目标岗位相关度、最近一次表现、重复失分次数、距离下一场面试的时间，以及关联任务是否已经验证完成；每一项优先级均需展示来源。

### 8.8 学习任务

任务来源包括 JD 差距、面试问题和用户手动创建。P0 不依赖 AI 自动生成。

任务字段包括任务名称、类型、关联技能或知识点、关联岗位或面试问题、优先级、预计耗时、截止时间、学习目标、验收标准、验证方式、验证结果、产出物链接，以及状态 `TODO / IN_PROGRESS / COMPLETED / ABANDONED`。

“部分答出”或“未答出”的问题支持一键创建任务。多道相同知识点问题可以关联同一任务。完成任务不会自动提升能力等级，也不自动消除薄弱点。用户通过自测、项目举例、口述演练或补充证据完成验证后，才能调整等级或降低该知识点的准备优先级。

### 8.9 能力与证据

默认能力分类包括 Java 基础、集合与并发、JVM、Spring/Spring Boot、MySQL、Redis、消息队列、微服务与分布式、Linux 与 Docker、系统设计、性能优化、问题排查和项目表达。

能力画像拆分为三个独立维度：

- `self_level`：用户自评等级 0–5。
- `evidence_status`：NO_EVIDENCE、WEAK、VALID。
- `interview_performance`：根据真实问题回答记录计算的统计结果。

JD 要求不得改变用户能力等级，任务完成也不得自动提升等级。

```text
0：不了解
1：了解概念
2：能够使用
3：能够独立实现
4：能够解释原理并解决问题
5：能够结合项目或生产场景进行设计
```

首次创建项目案例时，只要求“使用场景、采取的方案、解决的问题”三项摘要；结果、量化数据和外部链接均允许稍后补充。证据类型包括项目代码、Git 仓库、技术文章、架构图、接口文档、压测报告、日志或监控截图、面试回答和工作经历。每项证据应尽量回答在哪里使用、解决什么问题、采用什么方案、结果如何。没有数据时不得生成量化结果。

P0 证据以外部链接或用户填写的本地路径引用为主，不要求上传或复制原始代码和敏感附件。若后续支持附件上传，必须先定义文件大小、格式、存储位置、导出和敏感信息提示规则。

证据可以稍后补充，不作为首次分析 JD 的前置条件。

### 8.10 归档、删除与导出

- 岗位、投递、面试和项目默认归档或软删除。
- 删除前展示直接和间接关联数据数量。
- 被任务或简历引用的证据不能静默永久删除。
- 来源记录被删除后，学习任务保留，并标记“来源已删除”。
- P0 最近删除数据默认保留 30 天，期间允许恢复；复杂批量恢复和关联修复报告放入后续版本。
- 永久删除需要二次确认，完成后不可恢复。
- P0 支持导出岗位、投递、面试、复盘、问题、知识点、任务、技能和证据的完整 JSON 数据；面向分析的 CSV 导出放入后续版本。

## 9. P1 功能需求

### 9.1 可解释岗位匹配报告

- 必须要求和加分要求分别计算。
- 技能、经验和项目证据分开展示。
- 缺少资料不按零分处理。
- 保存权重、规则版本、生成时间和数据快照。
- 用户资料变化后，旧报告标记为可能过期。
- 投递建议展示理由，不自动替用户做决定。

### 9.2 AI 分析任务

AI 功能包括 JD 结构化提取、问题分类、回答质量分析和任务建议。所有 AI 调用使用异步任务状态：`QUEUED / RUNNING / SUCCEEDED / FAILED / CANCELED`。

AI 任务保存任务类型、业务对象及版本、模型版本、提示词版本、重试次数、失败原因、开始和完成时间、输出及用户确认状态。

AI 结果只创建候选变更。用户可以逐项接受、编辑或拒绝；重新生成不得覆盖已确认内容。

任务建议只读取问题、回答状态、已有改进信息和已关联知识点，生成一个可编辑的学习任务候选。用户采纳后才创建 `TODO` 任务及问题/知识点来源关联；AI 不自动创建任务、不新增知识点、不修改问题或技能。

### 9.3 浏览器与邮件提醒

- 用户主动授权后开启。
- 支持测试通知。
- 各渠道独立记录发送状态和失败原因。
- 权限被拒绝或发送失败时保留站内提醒。

### 9.4 简历定制

- 只能引用用户已确认的经历、技能和证据。
- AI 可以重写表达，但不能新增未经确认的项目、职责、指标或技术栈。
- 每条建议可追溯到来源事实。
- 用户确认前保持草稿状态，不覆盖主简历。

### 9.5 数据备份与恢复

支持导入 P0 导出的标准数据包、冲突预检、恢复预览和恢复结果报告；附件证据引用元数据随业务数据一并导入导出。

## 10. P2 功能需求

- 第三方日历双向同步。
- 飞书、钉钉、企业微信等通知渠道。
- AI 模拟面试和连续追问。
- 多份简历版本对比。
- 自动生成项目讲解稿。
- 高级趋势分析和不同简历、渠道的效果对比。
- 加密定时备份、跨设备同步和移动端深度优化。

样本量不足时不得输出可能误导用户的趋势结论。

## 11. 关键业务规则

### 11.1 状态转换

- 所有状态转换由专用业务操作完成，不允许通过通用更新接口任意改写。
- 非法状态转换不修改数据，并返回当前状态、目标状态和不允许原因。
- 状态历史不可被普通编辑操作覆盖。
- 请求支持幂等键，重复提交返回首次操作结果。

### 11.2 重复与合并

- 重复 JD、投递、技能或知识点先提示，不静默创建或合并。
- 技能采用标准名称与别名机制，例如不同大小写和常见简称可映射到同一技能。
- 合并后保留原始名称、来源和操作记录。

### 11.3 数据变化与结果失效

- 修改 JD 后，已有要求提取结果标记为待重新确认。
- 修改技能、证据或岗位要求后，相关 P1 匹配报告标记为可能过期。
- 修改面试时间时重新计算提醒。
- 删除来源事实后，引用内容保留但显示来源缺失或风险提示。

## 12. 数据模型

| 表名 | 用途 |
|---|---|
| user_profile | 用户信息、求职目标和时区 |
| skill | 标准技能、分类和别名 |
| user_skill | 用户自评等级及修改记录 |
| job_posting | 岗位、JD 原文、投递决定及理由 |
| job_requirement | 提取并经用户确认的岗位要求 |
| requirement_skill | 岗位要求与标准技能关联 |
| requirement_match | 要求匹配状态、依据和人工修正 |
| application_record | 投递记录、当前状态和下一步行动 |
| application_status_log | 投递状态变更历史 |
| interview_schedule | 面试日程状态和结果 |
| interview_reminder | 提醒计划、发送状态和重试信息 |
| interview_review | 快速或完整复盘 |
| interview_question | 面试问题和回答状态 |
| knowledge_point | 标准或自定义知识点 |
| question_knowledge | 问题与知识点关联 |
| learning_task | 学习任务、验收标准与验证结果 |
| task_source | 任务与岗位、问题等来源的关联 |
| project | 用户项目经历与最小项目案例摘要 |
| evidence | 项目、技能和经历证据引用 |
| skill_evidence | 技能与证据关联 |
| notification | 站内通知 |
| audit_log | 关键状态和确认操作记录 |
| ai_job | P1 AI 异步任务与审计信息 |

关键关系：

```text
岗位 1:N 岗位要求
岗位 1:N 投递记录
投递记录 1:N 面试日程
面试日程 1:1 当前复盘
复盘 1:N 面试问题
面试问题 N:M 知识点
学习任务 N:M 面试问题/岗位/技能/知识点
技能 N:M 证据
```

所有核心业务表包含创建时间、更新时间和可选删除时间。需要并发修改的记录包含版本号或等效的乐观锁字段。

## 13. 接口需求

### 岗位与要求

```text
POST   /api/jobs
GET    /api/jobs
GET    /api/jobs/{id}
PUT    /api/jobs/{id}
POST   /api/jobs/{id}/archive
POST   /api/jobs/{id}/restore
POST   /api/jobs/{id}/requirements/extract
GET    /api/jobs/{id}/requirements
PUT    /api/job-requirements/{id}
POST   /api/job-requirements/merge
GET    /api/jobs/{id}/gap-list
```

### 投递

```text
POST   /api/applications
GET    /api/applications
GET    /api/applications/{id}
PUT    /api/applications/{id}
POST   /api/applications/{id}/transition
GET    /api/applications/{id}/status-history
```

### 面试与提醒

```text
POST   /api/interviews
GET    /api/interviews/upcoming
GET    /api/interviews/{id}
GET    /api/interviews/{id}/preparation
PUT    /api/interviews/{id}
POST   /api/interviews/{id}/complete
POST   /api/interviews/{id}/cancel
POST   /api/interviews/{id}/reschedule
GET    /api/interviews/{id}/reminders
POST   /api/interviews/{id}/reminders
PUT    /api/reminders/{id}
```

### 复盘、问题与任务

```text
PUT    /api/interviews/{id}/review
GET    /api/interviews/{id}/review
POST   /api/reviews/{id}/complete
POST   /api/reviews/{id}/questions
PUT    /api/interview-questions/{id}
DELETE /api/interview-questions/{id}
POST   /api/interview-questions/{id}/create-task
GET    /api/knowledge-points/weak
GET    /api/tasks
POST   /api/tasks
PUT    /api/tasks/{id}
POST   /api/tasks/{id}/transition
```

### 能力、证据与数据管理

```text
GET    /api/skills/profile
PUT    /api/skills/{id}/self-level
GET    /api/evidence
POST   /api/evidence
PUT    /api/evidence/{id}
POST   /api/data-exports
GET    /api/data-exports/{id}
GET    /api/trash
POST   /api/trash/{id}/restore
DELETE /api/trash/{id}/permanent
```

### P1 AI 接口

```text
POST   /api/ai-jobs
GET    /api/ai-jobs/{id}
POST   /api/ai-jobs/{id}/retry
POST   /api/ai-jobs/{id}/cancel
POST   /api/ai-results/{id}/accept
POST   /api/ai-results/{id}/reject
```

状态转换和创建接口必须支持幂等请求键。

## 14. 技术方案建议

### 后端

- Java 17 或项目启动时可获得长期支持的稳定版本。
- Spring Boot。
- MyBatis-Plus 或 JPA，项目内统一选择一种主要持久化方案。
- 本地单用户优先使用嵌入式数据库（如 SQLite 或 H2），减少首次安装成本；需要局域网/公网部署或多设备数据服务时再使用 MySQL。
- Spring Scheduler 处理 P0 站内提醒；P0 只保证应用运行期间的提醒状态计算与进入应用后的可见性。
- OpenAPI/Swagger。
- 本地单用户模式不强制 Spring Security/JWT；公网部署前必须补充认证与授权。

Redis 不作为 P0 必需依赖。只有在明确需要分布式锁、缓存或多实例协调时引入。

### 前端

- Vue 3 + TypeScript 或 React + TypeScript，启动开发前固定一种技术栈。
- 桌面端优先，移动端保证核心流程基本可用。
- 统计图仅在数据量足以形成有效结论时展示。

### AI 服务

P1 统一封装 AI 服务接口。请求必须保存模型和提示词版本、业务对象版本、任务状态及用户确认结果。发送外部服务前支持敏感字段预览和脱敏。AI 失败不能影响手工功能。

## 15. 非功能与隐私需求

### 15.1 数据与隐私

- 默认不向外部 AI 服务发送数据。
- 启用 AI 前展示将发送的数据范围，并取得用户明确授权。
- 用户可排除姓名、公司、联系人、薪资和项目敏感字段。
- 应用日志不得记录完整 JD、简历、面试回答、访问令牌或 AI 请求正文。
- 本地路径和外部链接只作为用户提供的引用保存，应用不应自动读取、扫描或上传其指向的文件内容。
- 删除 AI 分析记录时，同时删除应用内保存的输入、输出和未确认结果。
- 导出数据不包含密钥、令牌和内部运行日志。

### 15.2 可靠性

- 创建、状态转换、提醒发送和 AI 任务创建支持幂等。
- AI、邮件或浏览器通知失败不影响手工数据保存。
- 所有失败状态提供可理解的原因和可执行的重试方式。
- 关键操作记录审计信息。

### 15.3 时间

- 时间点统一以 UTC 保存。
- 面试日程额外保存创建时的事件时区。
- 页面按用户当前时区展示，并明确跨时区事件的原始时区。

### 15.4 易用性与性能

- 核心表单支持草稿、跳过和稍后完善。
- 常规列表和详情操作在个人数据规模下应在 2 秒内给出结果或加载反馈。
- 页面满足键盘操作、清晰焦点和基础颜色对比要求。
- 桌面端完成全部功能，移动端至少支持查看日程、提醒、快速复盘和任务。

## 16. 指标定义

### 16.1 激活与使用指标

- **首次价值完成率**：创建首个岗位后，确认至少 3 项关键要求（候选项不足时确认全部）、查看差距清单并保存投递决定的用户比例。
- **岗位分析完成率**：至少确认一项必须要求且查看差距清单的岗位数 / 已创建岗位数。
- **下一步行动覆盖率**：进行中投递中具有未过期下一步行动的数量 / 进行中投递数量。
- **面试准备包使用率**：在面试开始前查看过准备包的已完成面试数 / 已完成面试数。
- **复盘完成率**：面试结束后 48 小时内完成复盘的面试数 / 已完成面试数。
- **任务转化率**：至少创建一个学习任务的复盘数 / 包含薄弱问题的已完成复盘数。
- **任务完成率**：完成任务数 / 到期任务数；已放弃任务单独展示。
- **任务验证率**：具有验证结果的已完成任务数 / 已完成任务数。

### 16.2 求职效果指标

- **简历通过率**：进入 RESUME_PASSED 及之后阶段的投递数 / 已投递且已有结果的投递数。
- **面试通过率**：结果为 PASSED 的已完成面试数 / 已填写结果的已完成面试数。
- **问题回答成功率**：完全答出问题数 / 已标记回答状态的问题数。
- **薄弱点改善**：同一知识点在两个可比较时间窗口中的加权薄弱次数变化；只有时间窗口、题目数量和目标岗位相关度足够可比时才展示。
- **Offer 周期**：首次创建有效投递到首次进入 OFFER 的自然日数。

指标必须显示分母和时间范围。样本量不足时展示原始数量，不输出趋势性结论。

## 17. P0 验收标准

### 17.1 岗位分析

- 用户不建立完整能力画像也能创建岗位并提取候选要求。
- 候选要求可以确认、编辑、合并、忽略和删除。
- 每项差距状态都可追溯到 JD 原文和用户资料。
- 未提供资料时显示“信息不足”，不显示“不满足”。
- 重复导入相同 JD 时给出提示，由用户决定是否继续。
- 首次使用时，用户无需建立完整能力画像，即可确认至少 3 项关键要求（候选项不足时确认全部）、查看差距清单并保存投递决定。

### 17.2 投递与状态

- 投递状态只能按合法状态转换。
- 重复提交相同幂等请求不会产生重复记录或状态日志。
- 岗位不重复保存投递状态。
- 状态历史可查看且包含发生时间。
- 进行中的投递可保存下一步行动与截止时间，首页可识别逾期或缺失行动。

### 17.3 面试与提醒

- 用户可创建、改期、完成和取消面试。
- 改期后旧提醒不再发送，新提醒按新时间生成。
- 取消面试后所有未发送提醒被取消。
- 同一提醒在本地单用户模式下最多展示一次。
- 用户进入应用后能看到已触发的站内提醒。

### 17.4 面试准备包

- 准备包能在不复制业务数据的情况下，聚合目标岗位要求、项目案例摘要、历史问题、未完成任务和准备事项。
- 每项优先准备内容均能显示其排序依据。
- 缺少项目案例或资料时显示待补充，不生成未经用户确认的内容。

### 17.5 复盘与学习任务

- 用户只填写问题和回答状态即可保存快速复盘。
- 草稿退出后可以继续编辑。
- 每道问题可关联多个知识点。
- 薄弱点统计可下钻到原始问题。
- “部分答出”或“未答出”的问题可一键创建任务。
- 多道问题可以关联同一学习任务。
- 完成任务不会自动修改能力等级。
- 已完成任务可以记录至少一种验证方式与验证结果。

### 17.6 删除、导出和失败处理

- 删除前展示影响范围，归档后历史记录仍可查看。
- 最近删除数据在 30 天内可恢复。
- 用户能导出完整业务数据，且导出中不包含密钥。
- AI 或外部通知不可用时，所有 P0 手工流程正常工作。
- 空数据、重复操作和非法状态转换均提供明确提示。

## 18. 版本规划

### V0.1：最小求职闭环

- 本地单用户模式。
- 岗位和 JD 基础要求提取、人工确认。
- 可解释差距清单，不提供综合百分比。
- 投递记录和状态历史。
- 投递决定、下一步行动和“今天应做什么”聚合。
- 面试日程、改期、取消和站内提醒。
- 单场面试准备包。
- 快速复盘、问题、知识点和薄弱点统计。
- 手工学习任务、问题一键转任务及验证结果。
- 最小项目案例与外部证据引用。
- 基础归档、恢复和完整 JSON 导出。

### V0.2：分析与效率增强

- 可解释匹配分数。
- AI 异步分析和候选变更确认。
- 浏览器通知与邮件提醒。
- 完整复盘分析。
- 简历定制草稿。
- 数据导入与完整恢复。
- CSV 导出、复杂恢复报告与完整附件证据库。
- 多实例提醒协调与更完善的失败重试策略。

### V0.3：面试与内容助手

- AI 模拟面试和连续追问。
- 项目讲解稿和高频追问准备。
- 多份简历版本对比。
- 更丰富的改善趋势分析。

### V1.0：跨端完整体验

- 第三方日历同步。
- 通知渠道扩展。
- 加密备份与跨设备同步。
- 移动端深度优化。

## 19. 开发顺序建议

1. 岗位创建、要求确认、差距状态和投递决定。
2. 投递记录、状态转换和下一步行动。
3. 面试日程、改期、取消和基础站内提醒。
4. 快速复盘、问题、知识点和学习任务。
5. 面试准备包及任务验证。
6. 最小项目案例、外部证据引用、归档、恢复和 JSON 导出。
7. P0 稳定后再接入 AI、外部通知、匹配分数和复杂恢复能力。

首期成功标准不是功能数量，而是用户能以较低录入成本完成至少一次完整闭环：

> 粘贴 JD → 做出投递判断 → 保存下一步行动 → 面试准备 → 完成复盘 → 创建并验证任务 → 下次面试前复习
