# JobHub 实现约束

本仓库用于实现本地单用户 Java 求职个人工作台。开始任何功能前，必须阅读以下文件，并按顺序解决冲突：

1. `docs/jobhub/IMPLEMENTATION_STATUS.md`：当前进度、正在进行的任务和下一步。
2. `docs/jobhub/IMPLEMENTATION_MASTER_PROMPT.md`：模型启动、窗口边界和交接规则。
3. `java-jobhub-prd.md`：产品范围、原则与优先级。
4. `docs/jobhub/02-state-machines.md`：状态转换唯一依据。
5. `docs/jobhub/03-openapi.yaml`：前后端接口唯一契约。
6. `docs/jobhub/04-database-design.md`：表语义、迁移和持久化规则。
7. `docs/jobhub/01-page-spec.md`：页面行为和用户路径。
8. `docs/jobhub/05-acceptance-test-cases.md`：验收与回归场景。
9. `docs/jobhub/06-technical-implementation.md`：固定技术方案与目录边界。

每次开始新窗口时，先根据 `IMPLEMENTATION_STATUS.md` 恢复上下文，并检查记录是否与当前代码一致。每次结束工作前，必须更新该文件，记录已完成内容、未完成内容、修改文件、验证结果和下一步；如果任务中途被打断，也要先写入当前进度再结束。

## 不可违反的产品规则

- 用户事实、人工确认和人工修正优先于规则/AI 推断；AI 不能静默覆盖任何用户事实。
- 未提供资料只能显示“信息不足”，不得推断为“不满足”。
- P0 在 AI、邮件、浏览器通知、第三方日历均不可用时必须可完整运行。
- 投递、面试、提醒、复盘和任务状态只能由专用业务命令变更；不得通过通用 `PUT` 改写状态字段。
- 完成学习任务不得自动提高技能等级，也不得自动清除薄弱点。
- 证据中的本地路径与外部链接仅作为文本引用保存；不得自动读取、扫描或上传其指向的内容。
- 本地单用户服务默认只监听回环地址；不得在未明确要求时加入登录、云同步、多租户或公网部署能力。

## 实现要求

- 前后端分离开发、单仓库管理。前端只通过 `/api` 访问后端；禁止前端直接访问数据库。
- 先更新 OpenAPI，再更新后端和前端。接口字段或枚举变化必须同步更新相关测试与页面。
- SQLite 结构变更必须新增 Flyway 迁移；禁止修改已执行迁移或依赖手工改库。
- 所有写操作实现幂等；所有可并发编辑资源使用 `If-Match-Version` 乐观锁。
- 所有时间点以 UTC 保存，面试额外保存事件时区；UI 负责按用户时区显示。
- 错误响应必须包含稳定错误码和可理解信息；非法状态转换不应产生数据副作用。
- 优先实现 V0.1，不提前实现 P1/P2 的 AI、邮件、推送、同步、复杂匹配评分或附件上传。

## 提交前检查

- 运行受影响的后端集成测试与前端静态检查。
- 对照 `05-acceptance-test-cases.md` 为新增或修改行为补充自动化测试。
- 检查 OpenAPI 引用、数据库迁移和枚举与状态机的一致性。
- 不提交数据库文件、导出数据、访问令牌、真实 JD、真实简历、面试回答或个人联系方式。
